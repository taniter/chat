import React, { Component } from 'react';

class Alerts extends Component {
    render() {
        return (
            <div className="alert show alert-custom alert-success">
                <strong>Success!</strong> Indicates a successful or positive action.
            </div>
        );
    }
}

export default Alerts;